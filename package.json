{
  "name": "brimcap",
  "version": "1.1.2",
  "bin": "build/dist/brimcap",
  "files": [
    "build/dist/**"
  ],
  "scripts": {
    "prepack": "make build",
    "postinstall": "chmod -R +x build/dist"
  }
}
