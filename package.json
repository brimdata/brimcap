{
  "name": "brimcap",
  "version": "1.1.2",
  "files": [
    "build/dist/**"
  ],
  "bin": {
    "brimcap": "build/dist/brimcap"
  },
  "scripts": {
    "prepack": "make build",
    "postinstall": "chmod -R +x build/dist"
  }
}
