script: brimcap analyze -suricata=F alerts.pcap | zq -Z 'count()' -

inputs:
  - name: alerts.pcap
    source: alerts.pcap

outputs:
  - name: stdout
    data: |
      {
          count: 379 (uint64)
      } (=0)
