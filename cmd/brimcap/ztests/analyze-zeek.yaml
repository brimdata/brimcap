script: brimcap analyze -analyzers.suricata.disabled alerts.pcap | zed query -Z 'count()' -

inputs:
  - name: alerts.pcap

outputs:
  - name: stdout
    data: |
      {
          count: 379 (uint64)
      }
