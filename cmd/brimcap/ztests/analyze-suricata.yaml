script: brimcap analyze -zeek=F alerts.pcap | zq -Z 'count()' -

inputs:
  - name: alerts.pcap
    source: alerts.pcap

outputs:
  - name: stdout
    data: |
      {
          count: 15 (uint64)
      } (=0)
