script: brimcap analyze -zeek=F alerts.pcap | zed q -Z 'count()' -

inputs:
  - name: alerts.pcap
    source: alerts.pcap

outputs:
  - name: stdout
    data: |
      {
          count: 15 (uint64)
      } (=0)
