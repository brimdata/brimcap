script: brimcap analyze -analyzer.zeek.disabled alerts.pcap | zed query -Z 'count()' -

inputs:
  - name: alerts.pcap

outputs:
  - name: stdout
    data: |
      {
          count: 15 (uint64)
      }
