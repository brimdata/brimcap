script: brimcap analyze alerts.pcap | zed q -Z 'count()' -

inputs:
  - name: alerts.pcap
    source: alerts.pcap

outputs:
  - name: stdout
    data: |
      {
          count: 394 (uint64)
      } (=0)
